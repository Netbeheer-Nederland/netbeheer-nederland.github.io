# Modellen

## Installatie op Windows zonder admin-rechten (portable modus)

1. Installeer [Ruby+Devkit](https://rubyinstaller.org/).
2. Draai `bundle install`.
3. Draai `bundle exec jekyll serve -s docs -d _site --livereload --incremental --open-url`.
